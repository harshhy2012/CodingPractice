#include<bits/stdc++.h>

using namespace std;

int main(){
    // ifstream cin("watermelon.txt");
    // if(cin.fail())
    // cout<<"Input from file failed";
    int w;
    cin>>w;
    if(w==2 || w%2!=0){
        cout<<"NO\n";
    }
    else{
        cout<<"YES\n";
    }
    //cin.close();
    return 0;
}